FROM node:24-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose the Vite port
EXPOSE 5173

# Run Vite in dev mode with --host so it's accessible outside the container
CMD ["npm", "run", "dev", "--", "--host"]